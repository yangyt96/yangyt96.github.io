# theme: jekyll-theme-slate

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
  -
    scope:
      path: "_posts"
      type: "post"
    values:
      layout: "Posts"
      author: "test"